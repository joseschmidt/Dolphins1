<%= simple_form_for @practice, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :practicedate, label: "Date" %>
  <%= f.association :group %>
  <%= f.input :lane %>
  <%= f.input :compstatus, label: "Complete?" %>
  <%= f.input :comment %>
  <%= f.button :submit %>

<% end %>
